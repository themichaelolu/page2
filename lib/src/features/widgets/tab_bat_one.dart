import 'package:flutter/material.dart';

class TabBarOne extends StatelessWidget {
  const TabBarOne({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('SetUp One'),
    );
  }
}
