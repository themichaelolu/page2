import 'package:flutter/material.dart';

class TabBarTwo extends StatelessWidget {
  const TabBarTwo({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('SETUP TWO'),
    );
  }
}
